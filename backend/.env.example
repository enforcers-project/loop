# Loop backend — copy to .env and fill in.
PORT=4000

# Sprint 1+ (not required for this in-memory build):
# DATABASE_URL=postgresql://loop:loop@localhost:5432/loop
# JWT_SECRET=change-me
# Backend-only AI/embeddings key (never expose to the frontend):
# EMBEDDINGS_API_KEY=
